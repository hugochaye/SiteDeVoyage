<form method="post" action="{{ path('circuit_like', {'id': circuit.id}) }}" onsubmit="return confirm('Are you sure you want to like this circuit?');">
    <input type="hidden" name="_method" value="LIKE">
    <input type="hidden" name="_token" value="{{ csrf_token('like' ~ circuit.id) }}">
    <button class="btn">Like</button>
</form>